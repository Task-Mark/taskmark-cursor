---
name: new-task
description: Create a new Taskmark task or bug under a story
---

# New task

Follow the `create-task` skill. Require a parent story. Create a task (`T-`) or bug (`B-`) under `items/`, calibrate t-shirt size from history, link from the story, and sync status/INDEX.
