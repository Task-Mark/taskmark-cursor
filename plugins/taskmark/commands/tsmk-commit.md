---
description: Commit Taskmark and linked project changes
---

Use the `commit-all` skill. Commit dirty linked repositories with concise,
repository-appropriate messages. Do not push unless the user explicitly asks.
