---
name: new-task
description: Create a new Taskmark task or bug (story optional; defaults to General/Unattached)
---

# New task

Follow the `create-task` skill. Prefer a parent story when named or clear from context; otherwise attach under General’s **Unattached** story. Create a task (`T-`) or bug (`B-`) under `items/`, calibrate t-shirt size from history, link from the story, and sync status/INDEX.
