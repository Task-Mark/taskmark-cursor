---
description: Cut a board SemVer and promote CHANGELOG Unreleased
---

Use the `tkmd-version` skill to cut a version on the Taskmark board.

This command never commits, pushes, tags, or publishes. It never bumps
`plugin.json`, marketplace metadata, or `@taskmark/ui`. It never writes the
board README.
