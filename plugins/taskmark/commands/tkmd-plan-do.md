---
description: Plan Taskmark work from prose, then implement the new items
---

Use the `tkmd-plan-do` skill.

This command never commits or pushes. It never introduces an `in_progress`
stage. Plan first like `/tkmd-plan`. If planning created new item files,
implement only that new work like `/tkmd-do`. If planning created nothing,
report the matching item and skip implementation.
