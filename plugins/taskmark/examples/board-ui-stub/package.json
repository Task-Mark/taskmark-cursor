{
  "name": "example-taskmark",
  "private": true,
  "type": "module",
  "description": "Board stub so `npm start` / `npx taskmark serve` launch the UI, `npm run build` emits static HTML, and `npm run preview` serves `out/`. Created by taskmark-init.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "taskmark serve --no-open",
    "serve": "taskmark serve",
    "dev": "taskmark dev",
    "build": "taskmark build --board . --out out",
    "preview": "taskmark preview"
  },
  "dependencies": {
    "@taskmark/ui": "^0.2.5"
  }
}
